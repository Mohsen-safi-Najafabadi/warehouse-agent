python q.py "SELECT * FROM suppliers LIMIT 5"
python q.py "SHOW TABLES"
python q.py "DESCRIBE deliveries"
python q.py "SELECT COUNT(*) AS n FROM deliveries"
python q.py "SELECT country, COUNT(*) AS n FROM suppliers GROUP BY country ORDER BY n DESC"
python q.py "SELECT status, COUNT(*) AS n FROM production_orders GROUP BY status"
python q.py "SELECT COUNT(*) AS not_delivered FROM deliveries WHERE actual_date IS NULL"
python q.py "SELECT COUNT(*) AS bad_qty FROM deliveries WHERE qty < 0"
python q.py "SELECT COUNT(*) AS orphans FROM parts p LEFT JOIN suppliers s ON p.supplier_id = s.supplier_id WHERE s.supplier_id IS NULL"
python q.py "SELECT name, COUNT(*) AS n FROM suppliers GROUP BY name HAVING COUNT(*) > 1"